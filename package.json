{
  "name": "localaudiobook",
  "private": true,
  "version": "1.0.0",
  "description": "Offline audiobook studio — turn any PDF/TXT/EPUB into an MP3 with Piper TTS",
  "type": "module",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "get:supertonic": "node scripts/get-supertonic.mjs",
    "get:kokoro": "node scripts/get-kokoro.mjs",
    "dev": "node scripts/dev.mjs",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "start": "npm run start --prefix backend",
    "lint": "npm run lint --prefix frontend",
    "clean": "npm run clean --prefix backend && npm run clean --prefix frontend"
  }
}
