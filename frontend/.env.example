# PORT: Port the Vite dev server listens on.
PORT=3000

# VITE_API_PROXY_TARGET: Where /api requests are proxied in development.
# Must match the backend's PORT.
VITE_API_PROXY_TARGET="http://localhost:3001"
